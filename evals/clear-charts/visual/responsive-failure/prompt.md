# Visual case: responsive failure

Audit the attached 360px-wide rendering. The desktop chart is 820px wide; overflow is clipped, and the legend sits outside the visible region. Return the Clear review contract.

Treat responsive behavior as part of comprehension and non-hover access, not as cosmetic polish.
